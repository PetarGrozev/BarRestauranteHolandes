RESUMEN DE CAMBIOS — 2026-04-25
Menú QR del cliente (mesa/[tableId])

ARCHIVOS MODIFICADOS
────────────────────
1. src/styles/globals.css
2. app/order/OrderPageClient.tsx
3. src/components/ProductCard.tsx

CAMBIOS CSS (globals.css)
─────────────────────────
- .menu-layout: añadido padding: 16px 12px 56px (fix edge-to-edge móvil) + gap: 20px
- .menu-hero: border-radius 10px→16px, gradiente simplificado, padding reducido
- .menu-hero-badge: forma de píldora (border-radius: 999px), más sutil
- .menu-hero-title: tamaño responsivo clamp(30px, 8vw, 44px) en lugar de 52px fijo
- .menu-hero-tagline: 16px→14px, menos opacidad
- Blobs decorativos hero: reducidos al 65% de tamaño original
- .menu-sections: gap reducido 40px→20px (móvil), 28px (desktop)
- .menu-section: padding 24px→16px, border-radius 8px→14px, fondo sutil
- .menu-section-copy h2: 28px→18px (móvil) / 22px (desktop)
- .menu-section-header: margenes reducidos
- .menu-product-grid: 2 columnas fijas en móvil, auto-fill desde 600px, 3 cols desktop
- .menu-cart: border-radius 10px→16px, fondo más limpio
- .menu-product-modal: border-radius 4px→18px
- .menu-bundle-card: border-radius 10px→14px
- Nuevo: .menu-cat-nav + .menu-cat-tab (nav de categorías horizontal con scroll)
- Nuevo: .menu-product-list + .menu-row-card + .menu-row-* (layout de filas para productos)
  - Fila: info (nombre+descripción+precio+botón+) a la izquierda, imagen 80x80px a la derecha
- Desktop 1024px+: padding layout ampliado, top sticky del carrito ajustado

CAMBIOS LÓGICA (OrderPageClient.tsx)
─────────────────────────────────────
- Nuevo estado: activeMenuTab (MenuCategory | 'MENUS' | null)
- useEffect: inicializa la tab activa al cargar productos (MENUS si existen, si no primera categoría)
- Nav de categorías añadida entre el hero y el menu-body
  - Tabs: Menu's (si existen), Burgers, Sandwiches, Pasta's, Dranken, Wijnen, Especialidades
  - Solo se muestra la sección de la tab activa (no todas a la vez)
- Secciones: antes mostraba todas las categorías a la vez; ahora solo la activa

CAMBIOS COMPONENTE (ProductCard.tsx)
─────────────────────────────────────
- Modo order+onPreview: rediseñado de tarjeta de cuadrícula a fila horizontal
  - Fila: izquierda → nombre, descripción, precio + botón "+"
  - Fila: derecha → imagen pequeña cuadrada (80x80px) si existe
  - Click en la fila abre el modal de detalle del producto
  - Click en el "+" añade al carrito sin abrir el modal
